// TODO: Replace server-rendered placeholders with live.json content when ready.
document.addEventListener('DOMContentLoaded', () => {
  document.documentElement.dataset.sportsLiveReady = 'true';
});
