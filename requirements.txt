# sports-trends core dependencies (lean; ML/data pipeline + frontend JSON).
# Heavy DL stacks (torch/tensorflow) are intentionally excluded — baseline
# models use scikit-learn. Pin loosely so CI stays reproducible but flexible.

pandas>=2.0
pyarrow>=14.0
requests>=2.31
pydantic>=2.4
python-dotenv>=1.0
huggingface_hub>=0.23
datasets>=2.18
scikit-learn>=1.3
joblib>=1.3
jinja2>=3.1
jsonschema>=4.20
pytest>=7.4
